class UserEntity {

  // String ? userId;
  String ? fullName;
  String ? email;
  String ? imageUrl;

  UserEntity({
    this.email,
    this.fullName,
    this.imageUrl,
    // this.userId
  });

}
